Subject: CK: FMHA attention-sink fixes

diff --git a/3rdparty/composable_kernel/include/ck_tile/ops/fmha/kernel/fmha_bwd_kernel.hpp b/3rdparty/composable_kernel/include/ck_tile/ops/fmha/kernel/fmha_bwd_kernel.hpp
index c666fc05f..6dfbc89c3 100644
--- a/3rdparty/composable_kernel/include/ck_tile/ops/fmha/kernel/fmha_bwd_kernel.hpp
+++ b/3rdparty/composable_kernel/include/ck_tile/ops/fmha/kernel/fmha_bwd_kernel.hpp
@@ -2310,7 +2310,7 @@ struct FmhaBwdOGradDotOKernel
         const LSEDataType sink_value =
             kargs.sink_ptr != nullptr
                 ? log2e_v<LSEDataType> *
-                      kargs.sink_ptr[static_cast<long_index_t>(i_batch) * kargs.nhead + i_nhead]
+                      kargs.sink_ptr[static_cast<long_index_t>(i_nhead)]
                 : -numeric<LSEDataType>::infinity();
 
         // for simplicity, batch stride we just modify the pointer
diff --git a/3rdparty/composable_kernel/include/ck_tile/ops/fmha/kernel/fmha_fwd_kernel.hpp b/3rdparty/composable_kernel/include/ck_tile/ops/fmha/kernel/fmha_fwd_kernel.hpp
index 2d0df7d3e..70f881964 100644
--- a/3rdparty/composable_kernel/include/ck_tile/ops/fmha/kernel/fmha_fwd_kernel.hpp
+++ b/3rdparty/composable_kernel/include/ck_tile/ops/fmha/kernel/fmha_fwd_kernel.hpp
@@ -1501,6 +1501,21 @@ struct FmhaFwdKernel
         return ck_tile::max(FmhaPipeline::GetSmemSize(), EpiloguePipeline::GetSmemSize());
     }
 
+    CK_TILE_DEVICE static constexpr float GetSoftmaxScale(const Kargs& kargs)
+    {
+        if constexpr(QScaleEnum == BlockAttentionQuantScaleEnum::PERTENSOR)
+        {
+            const float q_descale = *(reinterpret_cast<const float*>(kargs.q_descale_ptr));
+            const float k_descale = *(reinterpret_cast<const float*>(kargs.k_descale_ptr));
+
+            return kargs.scale_s * q_descale * k_descale;
+        }
+        else
+        {
+            return kargs.scale_s;
+        }
+    }
+
     CK_TILE_DEVICE void operator()(Kargs kargs) const
     {
         if constexpr(kIsAvailable)
@@ -1542,7 +1557,8 @@ struct FmhaFwdKernel
             {
                 sink_value =
                     kargs.sink_ptr != nullptr
-                        ? (*(static_cast<const float*>(kargs.sink_ptr) + i_nhead)) / kargs.scale_s
+                        ? (*(static_cast<const float*>(kargs.sink_ptr) + i_nhead)) /
+                              GetSoftmaxScale(kargs)
                         : -numeric<float>::infinity();
             }
 
@@ -2007,19 +2023,7 @@ struct FmhaFwdKernel
 
             AttentionVariant variant;
             const auto variant_params = [&] {
-                const float scale_s = [&] {
-                    if constexpr(QScaleEnum == BlockAttentionQuantScaleEnum::PERTENSOR)
-                    {
-                        float q_descale = *(reinterpret_cast<const float*>(kargs.q_descale_ptr));
-                        float k_descale = *(reinterpret_cast<const float*>(kargs.k_descale_ptr));
-
-                        return kargs.scale_s * q_descale * k_descale;
-                    }
-                    else
-                    {
-                        return kargs.scale_s;
-                    }
-                }();
+                const float scale_s = GetSoftmaxScale(kargs);
 
                 if constexpr(kHasLogitsSoftCap)
                 {
@@ -2354,7 +2358,8 @@ struct FmhaFwdKernel
             const auto [i_tile_m, i_tile_n, i_nhead, i_batch] = GetTileIndex(kargs);
             const float sink_value =
                 kargs.sink_ptr != nullptr
-                    ? (*(static_cast<const float*>(kargs.sink_ptr) + i_nhead)) / kargs.scale_s
+                    ? (*(static_cast<const float*>(kargs.sink_ptr) + i_nhead)) /
+                          GetSoftmaxScale(kargs)
                     : -numeric<float>::infinity();
 
             const index_t i_m0 = i_tile_m * FmhaPipeline::kM0;
diff --git a/3rdparty/composable_kernel/include/ck_tile/ops/fmha/pipeline/block_fmha_pipeline_qr_ks_vs.hpp b/3rdparty/composable_kernel/include/ck_tile/ops/fmha/pipeline/block_fmha_pipeline_qr_ks_vs.hpp
index 09b73c03d..9e972d89e 100644
--- a/3rdparty/composable_kernel/include/ck_tile/ops/fmha/pipeline/block_fmha_pipeline_qr_ks_vs.hpp
+++ b/3rdparty/composable_kernel/include/ck_tile/ops/fmha/pipeline/block_fmha_pipeline_qr_ks_vs.hpp
@@ -776,7 +776,7 @@ struct BlockFmhaPipelineQRKSVS
             }
             if constexpr(kHasSink)
             {
-                if(i_total_loops == 0)
+                if(i_total_loops == num_sink_loop - 1)
                     move_tile_window(bias_dram_window, {0, seqlen_k_start - sink_seq_end});
             }
             move_tile_window(bias_dram_window, {0, kN0});
@@ -1015,7 +1015,7 @@ struct BlockFmhaPipelineQRKSVS
                         return seqlen_k_start + i_total_loops * kN0;
 
                     const bool in_sink_phase = (num_sink_loop > i_total_loops);
-                    if(i_total_loops == num_sink_loop)
+                    if(num_sink_loop > 0 && i_total_loops == num_sink_loop)
                         move_tile_window(randval_dram_window, {0, seqlen_k_start - sink_seq_end});
 
                     return in_sink_phase ? (kv_load_start + i_total_loops * kN0)
@@ -1188,7 +1188,7 @@ struct BlockFmhaPipelineQRKSVS
             // move K tile windows
             if constexpr(kHasSink)
             {
-                if(i_total_loops == 0)
+                if(i_total_loops == num_sink_loop - 1)
                 {
                     move_tile_window(k_dram_block_window, {seqlen_k_start - sink_seq_end, 0});
                     move_tile_window(v_dram_window, {0, seqlen_k_start - sink_seq_end});
diff --git a/3rdparty/composable_kernel/include/ck_tile/ops/fmha/pipeline/block_fmha_pipeline_qr_ks_vs_async.hpp b/3rdparty/composable_kernel/include/ck_tile/ops/fmha/pipeline/block_fmha_pipeline_qr_ks_vs_async.hpp
index b621e9cea..826bf64fc 100644
--- a/3rdparty/composable_kernel/include/ck_tile/ops/fmha/pipeline/block_fmha_pipeline_qr_ks_vs_async.hpp
+++ b/3rdparty/composable_kernel/include/ck_tile/ops/fmha/pipeline/block_fmha_pipeline_qr_ks_vs_async.hpp
@@ -655,7 +655,7 @@ struct BlockFmhaPipelineQRKSVSAsync
             }
             if constexpr(kHasSink)
             {
-                if(i_total_loops == 0)
+                if(i_total_loops == num_sink_loop - 1)
                     move_tile_window(bias_dram_window, {0, seqlen_k_start - sink_seq_end});
             }
             move_tile_window(bias_dram_window, {0, kN0});
@@ -927,7 +927,7 @@ struct BlockFmhaPipelineQRKSVSAsync
                         return seqlen_k_start + i_total_loops * kN0;
 
                     const bool in_sink_phase = (num_sink_loop > i_total_loops);
-                    if(i_total_loops == num_sink_loop)
+                    if(num_sink_loop > 0 && i_total_loops == num_sink_loop)
                         move_tile_window(randval_dram_window, {0, seqlen_k_start - sink_seq_end});
 
                     return in_sink_phase ? (kv_load_start + i_total_loops * kN0)
